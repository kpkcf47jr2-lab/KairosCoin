# ═══════════════════════════════════════════════════════════════════════════════
#  KairosCoin Backend — Environment Variables
#  ⚠️  NEVER commit this file to git!
# ═══════════════════════════════════════════════════════════════════════════════

# ── Server ──────────────────────────────────────────────────────────────────
PORT=3001
NODE_ENV=production

# ── API Security ────────────────────────────────────────────────────────────
# Master API key for admin operations (mint/burn)
# Generate a strong key: openssl rand -hex 32
API_MASTER_KEY=CHANGE_ME_TO_A_STRONG_KEY

# Read-only API key for public endpoints (reserves, supply)
API_PUBLIC_KEY=CHANGE_ME_PUBLIC_KEY

# ── Blockchain ──────────────────────────────────────────────────────────────
# BSC RPC endpoint (use a paid one for production)
BSC_RPC_URL=https://bsc-dataseed1.binance.org

# Private key of contract owner (for mint/burn transactions)
# ⚠️  This is the MOST SENSITIVE value — protect it!
OWNER_PRIVATE_KEY=0xCHANGE_ME

# KairosCoin contract address on BSC
CONTRACT_ADDRESS=0x14D41707269c7D8b8DFa5095b38824a46dA05da3

# Chain ID (56 = BSC Mainnet, 97 = BSC Testnet)
CHAIN_ID=56

# ── Reserve Tracking ────────────────────────────────────────────────────────
# URL to verify USD bank reserves (optional — for proof of reserves page)
# RESERVE_VERIFICATION_URL=

# ── Rate Limiting ───────────────────────────────────────────────────────────
# Max requests per 15-minute window
RATE_LIMIT_MAX=100

# ── Auto Mint/Burn Engine ───────────────────────────────────────────────────
# Address that receives stablecoin deposits (USDT/BUSD/USDC on BSC)
# When stablecoins arrive → KAIROS auto-minted 1:1 to depositor
DEPOSIT_ADDRESS=0xYOUR_DEPOSIT_ADDRESS

# Address that receives KAIROS for redemption
# When KAIROS arrives → auto-burned → USDT sent back to user
REDEMPTION_ADDRESS=0xYOUR_REDEMPTION_ADDRESS

# Enable/disable auto engine
AUTO_ENGINE_ENABLED=true

# Poll intervals in ms (15000 = 15 seconds)
DEPOSIT_POLL_INTERVAL=15000
REDEMPTION_POLL_INTERVAL=15000

# ── Notifications (Optional) ───────────────────────────────────────────────
# Telegram bot token for alerts
# TELEGRAM_BOT_TOKEN=
# TELEGRAM_CHAT_ID=

# ── Transak Fiat On-Ramp ───────────────────────────────────────────────────
# Get these from your Transak partner dashboard: https://dashboard.transak.com
# API key for Transak widget integration
TRANSAK_API_KEY=
# API secret for server-side verification
TRANSAK_API_SECRET=
# Webhook secret for verifying webhook signatures
TRANSAK_WEBHOOK_SECRET=
# Environment: STAGING (for testing) or PRODUCTION (live)
TRANSAK_ENV=STAGING
